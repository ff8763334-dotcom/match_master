name: Build Flutter APK

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Unzip Project
      run: |
        if [ -f "match_master.zip" ]; then
          unzip match_master.zip -d ./
        else
          unzip *.zip -d ./
        fi

    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: 'stable'

    # خطوة إصلاح وتوليد ملفات الأندرويد والـ Gradle المفقودة تلقائياً داخل السيرفر
    - name: Find Project Root and Repair Project
      run: |
        PUBSPEC_PATH=$(find . -name "pubspec.yaml" -print -quit)
        if [ -z "$PUBSPEC_PATH" ]; then
          echo "Error: pubspec.yaml not found anywhere!" && exit 1
        fi
        PROJECT_ROOT=$(dirname "$PUBSPEC_PATH")
        echo "PROJECT_PATH=$PROJECT_ROOT" >> $GITHUB_ENV
        cd $PROJECT_ROOT
        
        # توليد مجلد أندرويد لكي يتعرف السيرفر على الـ Gradle بنجاح
        flutter create --platforms=android .
        flutter pub get

    - name: Build APK
      run: |
        cd ${{ env.PROJECT_PATH }}
        flutter build apk --debug

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Candy_Crush_APK
        path: "**/build/app/outputs/flutter-apk/app-debug.apk"
